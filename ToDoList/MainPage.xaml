<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoList.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackLayout x:Name="taskList" Grid.Row="0">
            <!-- Existing labels will go here -->
        </StackLayout>

        <Button Text="Create Task" Clicked="OnCreateTaskClicked" 
                Grid.Row="1" HorizontalOptions="Center" Margin="0,0,0,20"/>
    </Grid>

    <!-- 
    
 
    <VerticalStackLayout>

        <StackLayout x:Name="taskList">
            <Label x:Name="label1" Text="Test 1" HorizontalOptions="Center" VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnLabelTapped" />
                </Label.GestureRecognizers>
            </Label>
            <Label x:Name="label2" Text="Test 2" HorizontalOptions="Center" VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnLabelTapped" />
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>

        <Button Text="Create Task" Clicked="OnCreateTaskClicked" VerticalOptions="EndAndExpand" HorizontalOptions="Center" />




    </VerticalStackLayout>
       -->


</ContentPage>